<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.1"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">

    <persistence-unit name="reclamationPU" transaction-type="JTA">

        <!--
            DataSource JNDI configurée dans le serveur d'application (WildFly/GlassFish).
            À créer dans la console d'administration du serveur.
        -->
        <jta-data-source>java:jboss/datasources/ReclamationDS</jta-data-source>

        <!-- Classes entités gérées par JPA -->
        <class>com.reclamation.entities.Reclamation</class>
        <class>com.reclamation.entities.DetailsReclamation</class>

        <properties>
            <!-- Dialecte Hibernate pour la base de données -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>

            <!--
                hibernate.hbm2ddl.auto :
                - "create"      : Recrée le schéma à chaque démarrage (dev)
                - "update"      : Met à jour le schéma existant (test)
                - "validate"    : Valide uniquement (prod)
                - "create-drop" : Supprime le schéma à l'arrêt (test unitaire)
            -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- Affiche les requêtes SQL dans les logs (utile en développement) -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>

    </persistence-unit>
</persistence>
